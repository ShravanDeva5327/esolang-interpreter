                                                                        |
expression  : print(expr,...)?                                          |
			: identifier (EQUALS expression)?                           |
            : comp-expr ((AND|OR) comp-expr)*                           |
comp-expr   : NOT comp-expr                                             |
            : arith-expr ((ISE|NE|LT|GT|LTE|GTE) arit-expr)*            |   order of priority   
arith-expr  : term ((PLUS|MINUS) term)*                                 |
term        : power ((MUL|DIV) power)*                                  |         
power       : factor (POW factor)*                                      |            
factor      : INT|FLOAT|(expr)|identifier                               |
            : if-expr|for-expr|while-expr                               |         
                                                                        V

if-expr     : if expr then expr (elif expr then expr)* (else expr)? end
for-expr    : for identifier EQUALS expr to expr (step expr)? do expr
while-expr  : while expr do expr

# Variables

  var_name     =    expression
    ^          ^
identifier   equals